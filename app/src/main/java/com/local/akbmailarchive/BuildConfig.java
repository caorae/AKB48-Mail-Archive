package com.local.akbmailarchive;

/** Compatibility bridge for source files that still live in the original Java package. */
public final class BuildConfig {
    private BuildConfig() {}

    public static final String VERSION_NAME = com.azudaisuki.akbmailarchive.BuildConfig.VERSION_NAME;
}
